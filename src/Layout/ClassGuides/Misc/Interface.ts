
export interface MiscClassGuideInterface {
    id?: string;
    guideTitle: string;
    guideImage?: never; // Sanity image reference
    guideData: never; // Rich text content
    TestData: string;
    Link: string;
}
